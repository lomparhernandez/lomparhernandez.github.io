import React from 'react';
import ReactDOM from 'react-dom';

class MainApp extends React.Component {
  render() {
    return <h1>ECOM Store</h1>;
  }
}

ReactDOM.render(<MainApp />, document.getElementById("main"));
